<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Bottom navigation bar. -->
    <style name="BottomNavigationViewStyle" parent="Widget.MaterialComponents.BottomNavigationView.Colored">
        <item name="android:background">@color/background_secondary</item>
        <item name="itemTextColor">@color/bottom_navigation_color</item>
        <item name="itemIconTint">@color/bottom_navigation_color</item>
    </style>

    <!-- Action bar style. -->
    <style name="ActionBarStyle" parent="@style/Widget.MaterialComponents.ActionBar.Primary">
        <item name="background">@color/background_primary</item>
        <item name="android:titleTextStyle">@style/CustomTextLarge</item>
        <item name="titleTextStyle">@style/CustomTextLarge</item>
    </style>
</resources>